# Minimal CPU-only deps for worker_progress_test.py (no GPU / PyTorch / COLMAP)
#
# Python 3.7+ (Amazon Linux 2 ships python3 = 3.7). boto3 1.34+ needs Python 3.8+,
# so we cap boto3 below 1.34 on older AMIs. On Python 3.9+ you can use newer boto3.
#
# Install:
#   python3 --version   # should be 3.7+
#   pip3 install -r requirements-progress-test.txt

boto3>=1.26.0,<1.34
requests>=2.25.0,<3.0
urllib3>=1.26.0,<2.0
python-dotenv>=0.19.0
