[Unit]
Description=Route53 dynamic DNS for remote.openspacenexus.store
After=network-online.target
Wants=network-online.target

[Service]
Type=simple
ExecStart=%h/bin/route53-ddns.sh --daemon
Restart=always
RestartSec=10
Environment=ROUTE53_DDNS_INTERVAL=60

[Install]
WantedBy=default.target
