variable "api_gateway_name" {
  description = "Name of the REST API Gateway to attach to the api-dev.openspacenexus.store custom domain. Must match the name shown in AWS Console → API Gateway → APIs."
  type        = string
}
