# ----------------------------------------------------------------------
# Terraform
# ----------------------------------------------------------------------
.terraform/
*.tfstate
*.tfstate.*
*.tfvars
crash.log
debug.log
**/debug.log
override.tf
override.tf.json
*_override.tf
*_override.tf.json
.terraform.lock.hcl

# Lambda build artifacts
**/function_payload.zip
**/upload_payload.zip

# Review packaging (zip-source.sh output at repo root)
/*.zip

# ----------------------------------------------------------------------
# Python
# ----------------------------------------------------------------------
__pycache__/
*.py[cod]
*$py.class

# ----------------------------------------------------------------------
# Frontend (Next.js & Node)
# ----------------------------------------------------------------------
**/node_modules/
.next/
out/
frontend/redesign-preview.html

# Environment variables (Prevent secret leaks)
.env
.env.local
.env.development.local
.env.test.local
.env.production.local

# SSH / TLS private keys (never commit)
*.pem
.gaussian_worker.pem

# ----------------------------------------------------------------------
# OS / IDE
# ----------------------------------------------------------------------
.DS_Store
**/.DS_Store
.vscode/

# ----------------------------------------------------------------------
# graphify (generated knowledge graph — rebuild with: graphify . or graphify update .)
# ----------------------------------------------------------------------
graphify-out/

# ----------------------------------------------------------------------
# Claude Code — local machine config (not shared)
# ----------------------------------------------------------------------
.claude/settings.json
.claude/scheduled_tasks.lock

# ----------------------------------------------------------------------
# Old application code (to be removed after migration)
# ----------------------------------------------------------------------
oldApp/
site/
cursor_fix_prompt_scene_visibility_500.txt
cursor_dev_plan_splat_editor.txt
cursor_fix_prompt_visibility_500_round2.txt

# ----------------------------------------------------------------------
# Local working notes (not for commit — source material for later docs)
# ----------------------------------------------------------------------
TIER_PLAN_BATCHES.md
TIER_PLAN_BATCHES 08-07-2026.md
claude-code-user-management-prompt.txt
admin-user-management-changelog 08-07-2026.md
finnish_words_3000.json
Varjo_Teleport_Spec_Improvement_Report.md
